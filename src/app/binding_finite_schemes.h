#pragma once

// Register the built-in schemes whose first parameter has a finite catalog.
// Device-class finite schemes register through their device-class setup hooks.
void binding_finite_schemes_init();