// Thresholds component — nav-only registration (API wiring pending backend implementation)
#include "component_registry.h"
REGISTER_NAV_COMPONENT(thresholds, "thresholds", "sensors", "Thresholds", 20, "thresholds")
