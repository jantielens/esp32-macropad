#ifndef PAD_SCREEN_POLL_H
#define PAD_SCREEN_POLL_H

// Documentation marker for the pad_screen compilation-unit split.
// pad_screen_poll.cpp implements the PadScreen polling methods:
//   pollMqttBindings, pollColorBindings, pollNumberBindings,
//   pollBtnStateBindings, pollImageFrames.
// These remain methods of PadScreen — no separate class is introduced.
// This header exists so other files can #include it for documentation/navigation
// and so the split is self-describing without inspecting screens.cpp.

#endif // PAD_SCREEN_POLL_H
